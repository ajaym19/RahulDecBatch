package EncapsulationDemo;

public class EncapsulationNotes {

	/*
	 * Encapsulation
	 * -we are trying to hide the data
	 * -we wrap the variables and methods together
	 * 
	 * Access Modifiers
	 * private: can be accessed within the class only
	 * default: can be accessed only within the package
	 * protected: can be accessed within the package + by child class outside the package also
	 * public: can be accessed within the project
	 * 
	 * 
	 */
}
